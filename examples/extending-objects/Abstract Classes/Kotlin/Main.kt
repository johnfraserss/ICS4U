// Assuming everything is in the same package

fun main(args : Array<String>) {
	// val marge = Person("Marge", "Simpson", 36);  //this won't work
	val homer = Employee("Homer", "Simpson", 28, 1007);
	
	// println(marge.toString());  //this won't work
	println(homer.toString());
}